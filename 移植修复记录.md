# Slay the Spire 2 · iOS 移植修复与部署全流程记录

> 记录把 STS2 装到自有 iPhone 后遇到的一系列问题（崩溃 / 画面 / 声音 / 图标 / 速度）的根因、修复，以及从零到能玩的完整构建部署流程。
> 所有个人签名信息（Team ID、证书、设备 UDID）只在 `ios-export/config.sh`（已 gitignore）里，本文档不含。

---

## 一、问题总览

| # | 现象 | 根因（一句话） | 修复位置 |
|---|---|---|---|
| 1 | 选角色进游戏就崩：「内部错误 NullReferenceException」 | `SpineSprite` GDExtension 类型在 iOS 没注册，spine 节点退化成 `Node` | `deploy-slim.sh` 注入 spine framework |
| 2 | 主菜单背景、角色立绘都不显示 | 同 #1（所有 spine 渲染失效） | 随 #1 一起恢复 |
| 3 | 打开游戏没有声音 | FMOD 也是 GDExtension，它的库同样没进 app | `deploy-slim.sh` 注入 fmod framework |
| 4 | 游戏速度过快（4 倍速） | 移植补丁默认开了 4x 全局时间缩放 | `TimeScalePatch.cs` 改 `TARGET_SCALE` |
| 5 | App 图标是灰色空块 | Godot 导出用的是占位灰图 | `make-appicon.sh` 生成 + `deploy-slim.sh` 注入 |

---

## 二、核心原理：iOS 上 GDExtension 为什么会「消失」

问题 1/2/3 是**同一个根因**，理解它就理解了全部：

STS2 用两个 GDExtension——**Spine**（骨骼动画，管背景/角色/地图点）和 **FMOD**（音频）。在 iOS 上，GDExtension 的动态库（`.framework` / `.dylib`）有硬性约束：

1. **必须放在 `.app/Frameworks/` 里，并且被代码签名**，才能被 `dlopen` 加载。放在 pck 内的虚拟路径里加载不了。
2. 运行时 Godot 的 `OS_AppleEmbedded::open_dynamic_library()` 会把 `.gdextension` 里写的库路径逐级 fallback，**最终落到 `<app>/Frameworks/{name}.framework/{name}` 再 dlopen**。

而 **Godot 命令行导出不会自动把这两个 framework 塞进 app**。原因：Godot 导出进程跑在 macOS，它要「识别」一个 GDExtension、进而嵌入它的 iOS 库，前提是**能先加载这个扩展的 macOS 版库**（`.gdextension` 里的 `macos.editor` 条目）。我们手上只有 iOS 库、没有 macOS 库，于是导出时 `Verifying GDExtensions` 阶段 `dlopen` macOS 库失败 → 扩展没被加载 → 导出器不嵌入 iOS framework。

结果：签出来的 `.app/Frameworks/` 里只有 C# 的 `sts2.framework`，**缺 spine 和 fmod**。运行时这两个类型全部没注册：
- spine 节点 fallback 成基类 `Node` → 游戏拿它当 `SpineSprite` 用时报错 → 相关字段为 null → 进游戏崩溃
- fmod 音频类不存在 → 游戏调不到 → 没声音

**修复思路**：既然运行时就是去 `.app/Frameworks/` 找，那我们**手动把这两个 framework 拷进去、签名**即可，不必依赖 Godot 导出器自动嵌入，也不需要 macOS 库。这一步固化在 `deploy-slim.sh` 里，每次部署自动完成。

---

## 三、各问题根因与修复详解

### 1 & 2. 进游戏崩溃 + 画面不显示（Spine）

**调用链**（用诊断钩子抓到的完整堆栈）：
```
NGame.StartNewSingleplayerRun → StartRun → RunManager.EnterAct → SetActInternal
  → NMapScreen.RefreshAllPointVisuals()
    → NBossMapPoint.RefreshColorInstantly()   ← 这里抛 NullReferenceException
```

**为什么 null**：`NBossMapPoint._Ready()` 里
`_animController = new MegaSprite(_spineSprite)` → `_material = _animController.GetNormalMaterial()`。
`MegaSprite` 构造时 `MegaSpineBinding.ValidateBoundObject()` 检查 `BoundObject.GetClass() == "SpineSprite"`，实际拿到的是 `"Node"`，抛 `Expected BoundObject to be a SpineSprite, but it is a Node!`，`_material` 留 null；进游戏刷新地图时 `RefreshColorInstantly` 里 `_material.SetShaderParameter(...)` → NRE。

**根因**：`SpineSprite` 类型没注册（见第二节）。主菜单背景、角色立绘也都是 spine，所以一并黑掉。

**修复**：`deploy-slim.sh` 把
`addons/spine/ios/libspine_godot.ios.template_release.framework`
拷进瘦身包的 `Frameworks/`，随后统一签名。装上后 `Expected SpineSprite` 报错归零、背景/角色/地图全部恢复、进游戏不再崩。

### 3. 没有声音（FMOD）

**同类根因**：fmod GDExtension 的库没进 `.app/Frameworks/`，音频类没注册。

**FMOD 的特殊性**（诊断确认）：它的 iOS 产物是 xcframework 里的一个裸 dylib（`libGodotFmod.ios.template_release.universal.dylib`），已经**静态包含 FMOD 引擎**（`otool -L` 无外部 fmod 依赖），只有两个 undefined 符号：
- `_fmodf`：数学函数，系统库解析
- `_load_all_fmod_plugins`：由 `build-ios.sh` step5.2 注入到 `dummy.cpp`、编进主二进制的空实现

**修复**：`deploy-slim.sh` 把这个 dylib 包装成标准 framework 结构
`libGodotFmod.ios.template_release.framework`（自建 `Info.plist` + `install_name_tool -id @rpath/...`）注入 `Frameworks/` 并签名。装上后声音恢复。
（注意 iOS 侧边**静音拨片**开着时游戏依然可能没声，这不是 bug。）

### 4. 游戏速度过快（4x → 1x）

不是 bug，是移植补丁 `src/STS2MobileIos/Patches/TimeScalePatch.cs` 的设计：用 `Engine.TimeScale` 做全局 4 倍速（游戏自带调试功能的上限就是 4.0，**不改任何战斗数值**，只是动画/计时整体播快），并处理了 Boss 二阶段慢动作叠加。

**调整**：改常量 `TARGET_SCALE`（`1.0` = 原速，`2.0` = 适中）。本次已改回 **1.0 原速**。改后需重新 `build-ios.sh` + `deploy-slim.sh`。

### 5. App 图标（灰块 → 官方封面）

Godot 导出要求项目里有 App 图标，最初用的是临时生成的 128×128 灰图占位。iOS 主屏图标读的是编译过的 `Assets.car`（不是散图标 PNG），所以换图标要重新编译 asset catalog。

**方案**：`tools/make-appicon.sh` 从一张方形源图生成全套尺寸 → `actool` 编译成 `Assets.car` + 散图标，产物落 `ios-export/appicon/`；`deploy-slim.sh` 在签名前把它们覆盖进 app。
图标源优先级：`make-appicon.sh` 的命令行参数 > `ios-export/appicon-src.png`（自备方形图）> 游戏 `icon.icns`。
当前用的是从 IGN 抓的**官方封面主视觉**（1024×1024），存在 `ios-export/appicon-src.png`。想换任何图，把方形图覆盖那个文件、重跑脚本即可。

---

## 四、安装部署全流程

### 0. 前置准备（一次性）
- macOS + Xcode（登录免费 Apple ID）
- Godot 4.5.1 **mono** 版 + iOS 导出模板
- .NET 9 SDK
- 自备两个第三方 iOS 库（本仓库不转发，见 `docs/THIRD_PARTY_LIBS.md`）：
  - `libspine_godot.ios.template_release.framework`（Spine 4.2 / Godot 4.5.1）
  - `libGodotFmod.ios.template_release.xcframework` + FMOD 引擎静态库 `libfmod{,studio}_iphoneos.a`
- 合法拥有的 STS2（Steam macOS 版，提供游戏 dll / pck / 资源）
- 复制 `ios-export/config.example.sh` 为 `ios-export/config.sh`，填入自己的 Team ID / bundle id / 签名身份 / 设备 UDID / 各路径

### 1. 完整构建 `ios-export/build-ios.sh`
六步（幂等，缺啥响亮报错）：
1. **织入**：Mono.Cecil 把移植补丁静态织进游戏 `sts2.dll`（iOS 禁 JIT，不能用运行时 Harmony）
2. **组装依赖**：挑出第三方托管 dll
3. **放 GDExtension iOS 库**到导出工程 `addons/`
4. **NativeAOT**：`dotnet publish` 把织入后的程序集预编译成 `sts2.dylib`（→ `sts2.framework`）
5. **Godot 导出** Xcode 工程 + 修 pbxproj 签名 + 注入 FMOD 空插件 + 写大内存 entitlement
6. **pck 三连**：移 sentry + 补 672 个 `.cs` 脚本占位（官方引擎能加载 MegaDot 私有引擎打的 pck）

产物：`ios-export/build/StS2.xcodeproj`、`sts2.dylib`、`build/StS2.pck`(~1.8G)。

### 2. 首次签名装机（Xcode GUI，免费账号必须）
1. Xcode 打开 `ios-export/build/StS2.xcodeproj`
2. Signing & Capabilities：选账号、勾自动签名、加 **Increased Memory** capability（突破 iOS 单 app 内存上限，否则预热/存档时 OOM 被杀）
3. 选 iPhone → ▶ Run（编译 + 签名 + 装机）
4. 把 `build/StS2.pck` 推到 iPhone 的 `Documents/StS2.pck`（内容与签名分离，让瘦身壳可反复重签）

### 3. 生成 App 图标（可选，一次性）
```bash
# 想用自定义图：先把 1024×1024 方形图放到 ios-export/appicon-src.png
bash tools/make-appicon.sh              # 默认用 appicon-src.png，没有则回退游戏 icns
bash tools/make-appicon.sh /path/to.png # 或直接指定源图
```

### 4. 增量部署 `ios-export/deploy-slim.sh`（日常迭代）
一条命令完成，**保留手机上的 pck 和存档**（覆盖安装，绝不 uninstall）：
1. 把新的 `sts2.dylib` 换进 app
2. 剥掉内置 pck、注入 `--main-pack user://StS2.pck`（走文档区大 pck）
3. **注入 spine framework**（修复 #1/#2）
4. **注入 fmod framework**（修复 #3）
5. **换 App 图标**（修复 #5，`ios-export/appicon/` 存在时）
6. 逐个 framework + app 签名（含大内存 entitlement）
7. USB 装机，并核验文档区 pck 仍在

> 改了 C# 补丁 → 跑 `build-ios.sh` 再 `deploy-slim.sh`。
> 只想重装不改代码 → 直接 `deploy-slim.sh`。
> spine/fmod/图标都是 deploy-slim 自动带上的**持久修复**，不用每次手动。

---

## 五、诊断技巧（排查时用得上）

- **抓 app 的 stdio**（普通 idevicesyslog 抓不到独立启动的 app）：
  ```bash
  xcrun devicectl device process launch --console --terminate-existing \
    --device <UDID> <bundle-id>
  ```
  **要求 iPhone 解锁且亮屏**（否则启动被拒；建议把自动锁定设为「永不」）。
- **抓崩溃完整堆栈**：iOS NativeAOT 没有运行时 Harmony，游戏的错误弹窗又只显示消息。曾在 `PlatformPatches.InitializePlatformPrefix` 里临时装 `AppDomain.CurrentDomain.FirstChanceException` 钩子，在任何 catch 之前打印异常 `ToString()`（含堆栈）。根因确认后已移除。
- **反编译游戏程序集**定位空引用字段：`ilspycmd`（装的是需要 .NET 8 的版本时，用 `DOTNET_ROLL_FORWARD=Major` 跑在 .NET 9 上）。
- **验证签名保住了大内存权限**：容器放个 `memcheck_on` 文件，补丁会把可用内存额度写进 `memcheck.txt`（有权限 ≈7GB+，没有 ≈3GB）。

---

## 六、本次改动的文件

| 文件 | 改动 |
|---|---|
| `ios-export/deploy-slim.sh` | 新增注入 spine framework、fmod framework、App 图标三步 |
| `src/STS2MobileIos/Patches/TimeScalePatch.cs` | `TARGET_SCALE` 4.0 → 1.0（原速） |
| `tools/make-appicon.sh` | 新增：从方形源图生成 iOS App 图标（`actool` 编 `Assets.car`） |
| `ios-export/appicon-src.png` | 新增（gitignore）：图标源图，当前为官方封面主视觉 |
| `ios-export/appicon/` | 新增（gitignore）：生成的图标产物 |

---

## 七、当前状态

进游戏、主菜单画面、角色立绘、地图、声音全部正常，速度为原速 1x，图标为官方封面。一次 `deploy-slim.sh` 自动带上所有修复。移植核心完成，可正常游玩。
